#include<bits/stdc++.h>
using namespace std;
 
int main() {
    int n;cin>>n;
    vector<int> arr(n);
    for(int i=0 ; i<n ; i++){
        int m;cin>>m;
        arr[n-i-1] = m;
    }
    for(int i:arr) cout << i << ' ';
    cout << endl;
  return 0;
}